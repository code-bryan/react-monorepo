{
  "name": "react-monorepo",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private":"true",
  "scripts": {
    "web:start": "yarn workspace web start",
    "ext:start": "yarn workspace extension start"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
